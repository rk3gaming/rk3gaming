# Configuration

```lua
Config = {}

--Config.Framework = 'esx' Not needed do NOT uncomment it out, script is standalone.

Config.MarkerSize = {x = 0.5, y = 0.5, z = 0.5} 

Config.MarkerColor = {r = 255, g = 255, b = 0, a = 100}  -- dont change the A color.

-- command list
-- /cim - available for anyone
-- /markers - admin command to delete markers (IC) need to setup CFG perms for this.
```
